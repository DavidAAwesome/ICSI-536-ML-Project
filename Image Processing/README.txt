Put xray images into 'input_images' folder (the one currently there are used for example)
Output should generate augmented images of the images in the 'input_image' folder into the 'augmented_images' folder
(ignore the blank file in 'augmented_images' folder, its just so i can add a blank folder)
